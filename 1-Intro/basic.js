/*This Code can be executed either by using html on the browers console(F12) 
Or by using Node.js a javascript run-time environment and see code directly on the terminal*/
console.log("Hello World!")
console.log("Hello World!")
console.clear()//will clear the console (above messages) 
console.log("Hello World!") //to display text on console 
alert("This is an Alert!!!")// for alerts
console.warn("This is a Warning")//for warning in console
console.error("This is an Error")//error message 
/* object.method() 
 console is an objects
log(),warn(),clear() ... etc are it's methods
 . is used to access those methods defined  for the object */

// This is a Comment it won't execute it is sort of a message

// variables a container which holds data
// reserved keyword unique name =  data 
let name; // initializing a variable
console.log(name); // -> undefined as no value assigned
let Name = "Umer Adnan" // assigning the value
console.log(Name);
let age = 19;
console.log(age);
// can't use reserved keywords as variable name such as for,var,class
// variables can't have spaces between variable name can use underscore
// can't use numbers first in name or any special characters in variable name
let gender = "MALE";
gender = "male" // variables value is changed
const NAME = "Muhammad Umer Adnan" // const makes the value unchangable (constant)
// if you try to change then it will cause error
console.log(NAME);
//Data Types
let A = "a"
let B = 1
let C = 3.9
let D = true
console.log(` "a" = ${typeof A}`); // concatination writing strings and different data types together
console.log(` 1 = ${typeof B}`);
console.log(` 3.9 = ${typeof C}`);
console.log(` true = ${typeof D}`); // -> boolean : true is 1 or false is 0

// false values which result in false
/* false
   0 , - 0
   NaN -> not a number
   null
   undefined
   '',"",`` 
 */
// trial
//document.body.innerHTML = "I am Learning JavaScript Again!"; 






